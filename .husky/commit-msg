# CHANGELOG.md updates now happen via scripts/commit.mjs (npm run commit),
# staged before `git commit` runs so they land in the same commit. This hook
# only warns if a bare `git commit` was used and an entry would have been
# skipped — see the `changelog` skill for details.
node scripts/warn-changelog.mjs "$1"

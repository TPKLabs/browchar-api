# 1. Validate the message follows Conventional Commits (see commitlint.config.mjs)
npx --no-install commitlint --edit "$1" &&
  # 2. Append CHANGELOG.md entries, staged into the same commit
  node scripts/update-changelog.mjs "$1"

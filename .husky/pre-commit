# Lint + format staged files, and enforce paired specs (via lint-staged)
npx lint-staged &&
  # Type check the whole project
  npx tsc --noEmit &&
  # Validate the Prisma schema
  npx prisma validate &&
  # Run unit tests, stop on first failure
  npx jest --passWithNoTests --bail
